# Example Prometheus scrape config. Copy into your prometheus.yml scrape_configs.
scrape_configs:
  - job_name: mnemosyne
    scrape_interval: 15s
    static_configs:
      - targets: ["localhost:9090"]
        labels:
          service: mnemosyne
          env: dev
